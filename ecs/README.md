# ECS Task Definitions

Task definitions for ECS Fargate deployment.

## Files
- task-definition-backend.json - Backend (Node.js)
- task-definition-frontend.json - Frontend (React)

## Variables
- ${AWS_ACCOUNT_ID}, ${AWS_REGION}, ${ECR_REGISTRY}, ${IMAGE_TAG}, ${MONITORING_PRIVATE_IP}
